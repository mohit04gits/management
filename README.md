# CareFlow React Hospital Management System

Run:
1. Install Node.js
2. Extract this folder
3. Run `npm install`
4. Run `npm run dev`
5. Open the Vite URL shown in the terminal.

Production: `npm run build` then `npm run preview`.

React + Vite + Lucide icons. No backend/database required; data uses localStorage.
